import { Vec2Like } from "./Vec2Like";
/**
 * 2次元ベクトル。
 */
export declare class Vec2 {
    /**
     * 零ベクトル。
     */
    static readonly zero: Readonly<Vec2Like>;
    /**
     * x, y 要素をコピーする。
     *
     * @param v1 コピー先ベクトル。
     * @param v2 コピー元ベクトル。
     */
    static copy(v1: Vec2Like, v2: Vec2Like): Vec2Like;
    /**
     * 二つのベクトルを比較する。
     *
     * ベクトルが等しい時、真。
     *
     * @param v1 ベクトル。
     * @param v2 ベクトル。
     */
    static equal(v1: Vec2Like, v2: Vec2Like): boolean;
    /**
     * 二つのベクトルを加算する。
     *
     * @param v1 ベクトル。結果はこのベクトルに格納される。
     * @param v2 ベクトル。
     */
    static add(v1: Vec2Like, v2: Vec2Like): Vec2Like;
    /**
     * 二つのベクトルを減算する。
     *
     * @param v1 ベクトル。結果はこのベクトルに格納される。
     * @param v2 ベクトル。
     */
    static sub(v1: Vec2Like, v2: Vec2Like): Vec2Like;
    /**
     * 内積を求める。
     *
     * @param v1 ベクトル。
     * @param v2 ベクトル。
     */
    static dot(v1: Vec2Like, v2: Vec2Like): number;
    /**
     * 外積を求める。
     *
     * @param v1 ベクトル。
     * @param v2 ベクトル。
     */
    static cross(v1: Vec2Like, v2: Vec2Like): number;
    /**
     * 二つの単位ベクトルが平行か調べる。
     *
     * 二つのベクトルの内積が閾値以上の時、真（平行）。
     *
     * @param v1 単位ベクトル。
     * @param v2 単位ベクトル。
     * @param threshold 閾値。省略時 1 。
     */
    static parallel(v1: Vec2Like, v2: Vec2Like, threshold?: number): boolean;
    /** X要素。 */
    x: number;
    /** Y要素。 */
    y: number;
    /**
     * コンストラクタ。
     *
     * @param x X要素初期値。
     * @param y Y要素初期値。
     */
    constructor(x: number, y: number);
    /**
     * コンストラクタ。
     *
     * @param vec2Like 初期値。省略時、零ベクトル。
     */
    constructor(vec2Like?: Vec2Like);
    /**
     * x, y 要素をこのベクトルにコピーする。
     *
     * @param v コピー元ベクトル。
     */
    copy(v: Vec2Like): this;
    /**
     * 複製する。
     */
    clone(): Vec2;
    /**
     * 比較する。
     *
     * 等しい時、真。
     *
     * @param v ベクトル。
     */
    equal(v: Vec2Like): boolean;
    /**
     * このベクトルと単位ベクトルが平行か調べる。
     *
     * このベクトル自身が単位ベクトルでなければならない。
     *
     * 二つのベクトルの内積が閾値以上の時、真（平行）。
     *
     * @param v 単位ベクトル。
     * @param threshold 閾値。省略時 1 。
     */
    parallel(v: Vec2Like, threshold?: number): boolean;
    /**
     * 加算する。
     *
     * @param v ベクトル。
     */
    add(v: Vec2Like): this;
    /**
     * 減算する。
     *
     * @param v ベクトル。
     */
    sub(v: Vec2Like): this;
    /**
     * スケールする。
     *
     * @param v スケール。
     */
    scale(v: number): this;
    /**
     * アダマール積を求める。
     *
     * @param v ベクトル。
     */
    mul(v: Vec2Like): this;
    /**
     * 内積を求める。
     *
     * @param v ベクトル。
     */
    dot(v: Vec2Like): number;
    /**
     * 外積を求める。
     *
     * @param v ベクトル。
     */
    cross(v: Vec2Like): number;
    /**
     * 投影する。
     *
     * @param v 投影先のベクトル。
     */
    project(v: Vec2Like): this;
    /**
     * 投影したベクトルから自身へ向かうベクトルにする。
     *
     * @param v 投影先のベクトル。
     */
    reject(v: Vec2Like): this;
    /**
     * 反射する。
     *
     * @param n 法線ベクトル。
     */
    reflect(n: Vec2Like): this;
    /**
     * 長さの二乗を求める。
     */
    squaredLength(): number;
    /**
     * 長さを求める。
     */
    length(): number;
    /**
     * 正規化する。
     */
    normalize(): this;
    /**
     * 符号を反転する。
     */
    negate(): this;
    /**
     * 四捨五入する。
     */
    round(): this;
    /**
     * 小数点以下を切り捨てる。
     */
    floor(): this;
    /**
     * 小数点以下を切り上げる。
     */
    ceil(): this;
    /**
     * 各要素を符号を表す +/- 1, +/- 0 にする。
     */
    sign(): this;
    /**
     * 回転する。
     *
     * @param angle 回転の角度[radian]。
     */
    rotate(angle: number): this;
    /**
     * 90度回転する。
     */
    rotate90(): this;
    /**
     * 270度回転する。
     */
    rotate270(): this;
}
