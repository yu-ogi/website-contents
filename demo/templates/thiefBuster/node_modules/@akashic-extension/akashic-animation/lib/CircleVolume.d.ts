import { AABB } from "./AABB";
import { Vector2 } from "./Vector2";
import type { Volume } from "./Volume";
/**
 * CircleVolume。
 *
 * アタリ判定用真円形Volume。
 */
export declare class CircleVolume implements Volume {
    /**
     * 位置（ワールド座標）。
     */
    pos: Vector2;
    /**
     * 半径。
     */
    r: number;
    aabbFirst: boolean;
    dirty: boolean;
    private _aabb;
    constructor();
    aabb(): AABB;
}
