"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.CircleVolume = void 0;
const AABB_1 = require("./AABB");
const Vector2_1 = require("./Vector2");
/**
 * CircleVolume。
 *
 * アタリ判定用真円形Volume。
 */
class CircleVolume {
    constructor() {
        this.pos = new Vector2_1.Vector2();
        this.r = 0;
    }
    aabb() {
        if (!this._aabb || this.dirty) {
            this.dirty = false;
            if (!this._aabb)
                this._aabb = new AABB_1.AABB();
            this._aabb.origin.x = this.pos.x;
            this._aabb.origin.y = this.pos.y;
            this._aabb.extent.width = this.r;
            this._aabb.extent.height = this.r;
        }
        return this._aabb;
    }
}
exports.CircleVolume = CircleVolume;
