import { AABB } from "./AABB";
import { Size2 } from "./Size2";
import { Vector2 } from "./Vector2";
import type { Volume } from "./Volume";
/**
 * BoxVolume
 *
 * アタリ判定用矩形Volume。
 *
 * 実際にアタリ判定を行うときは `BoxVolume#matrix`,`origin` そして, `size` を用いてワールド座標系での領域を求める必要があります。
 */
export declare class BoxVolume implements Volume {
    /**
     * 矩形の原点（ローカル座標）
     */
    origin: Vector2;
    /**
     * 矩形のサイズ（スケール前）
     */
    size: Size2;
    /**
     * ワールド座標に変換する行列
     */
    matrix: g.Matrix;
    aabbFirst: boolean;
    dirty: boolean;
    private _aabb;
    constructor();
    aabb(): AABB;
}
